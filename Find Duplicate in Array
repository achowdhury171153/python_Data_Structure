class Solution:
   def containsDuplicate(self, nums: List[int]) -> bool:
        lenA= len(nums)
        unique=set(nums)
        lenB=len(unique)
        #print(unique)
        #print(lenA)
        #print(lenB)
        if lenA!=lenB:
            return True
        else:
            return False
