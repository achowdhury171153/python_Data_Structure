class Solution:
    def merge(self, nums1: List[int], m: int, nums2: List[int], n: int) -> None:
        k=0
       
        new=[]
        
        
        for j in range(0,n):
            nums1[m]=nums2[j] 
            m=m+1
          
        
        nums1.sort()   
        print(nums1)
